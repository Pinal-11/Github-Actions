Basic Structure of a GitHub Actions Workflow

A workflow has:

name: → Name of your workflow
on: → When it should trigger (push, pull_request, schedule, manual)
jobs: → What jobs (sets of steps) it should run
steps: → Actual individual actions/tasks like checkout code, run scripts, deploy
